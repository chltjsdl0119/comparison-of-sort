/**
 * 배열을 생성할 때, 어떤 배열을 생성할지 정할 수 있는 열거형
 *
 * @Type SORTED 오름차순으로 정렬된 배열을 생성.
 * @Type REVERSE 내림차순으로 정렬된 배열을 생성.
 * @Type RANDOM 각 원소를 무작위로 배열을 생성.
 */
public enum ArrayType {
    SORTED,
    REVERSE,
    RANDOM
}
